# Copyright (c) 2019. TIBCO Software Inc.
# This file is subject to the license terms contained
# in the license file that is distributed with this file.

# environment variable file for docker run and docker-compose 
# used in entrypoint.sh

# user, password and other details for JasperReports Server repository

DB_TYPE=mysql
DB_HOST=172.22.51.41
DB_PORT=3306
DB_USER=jaspersoft
DB_PASSWORD=jaspersoft

# default database name
# change JasperReports Server repository database name, for example:
DB_NAME=jasperserver_db

# important option here is the timezone
JAVA_OPTS=-Xmx3g -XX:MaxMetaspaceSize=378m -XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap -Duser.timezone=Europe/Berlin

# set Jasperserver to SSL for SSL-only environments => bei WEBASTO bspw. muss das hier auf true
SERVER_XML_USE_SECURE=true

# If clustering. Additional customizations in JasperServer and Tomcat needed
#CLUSTERED=false

#this cannot be set to true in a customer environment since we won't give jasperserver full access to the database
#loading samples requires the right to add users etc.
JRS_LOAD_SAMPLES=true

# If running on windows and using volumes
#COMPOSE_CONVERT_WINDOWS_PATHS=1

# regenerate JasperReports Server JNDI database configuration
# and JDBC driver in Tomcat
# JRS_DBCONFIG_REGEN=true

# only run JRS in HTTPS. Cannot be turned off
# JRS_HTTPS_ONLY=true

JDBC_DRIVER_VERSION=1.6.3

# if keystore is loaded via volume
# HTTPS_PORT
# KS_PASSWORD
# DN_HOSTNAME only in Dockerfile for the self signed certificate


